# A-Transformer-Variant-with-Topological-Data-Analysis

Variations of the transformer architecture as described in the associated paper "A Transformer Variant with Topological Data Analysis."

Only the code is uploaded here. The trained models are quite large and not uploaded to this github repository.

## Installation

All required packages can be installed with:
pip install -r requirements.txt
